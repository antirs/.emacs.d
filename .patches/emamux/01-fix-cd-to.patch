--- a/emamux.el	2023-08-04 15:01:35.203402101 +0300
+++ b/emamux.el	2023-08-04 13:28:54.151523653 +0300
@@ -479,11 +479,11 @@
            (unless (string-match tramp-local-host-regexp host)
              (setq ssh-to host)
              (setq ssh-user user)))
-       (setq cd-to default-directory))
+       (setq cd-to (expand-file-name default-directory)))
      (let ((default-directory (expand-file-name "~")))
        ,@body
        (let ((new-pane-id (emamux:current-active-pane-id))
-             (chdir-cmd (format " cd %s" cd-to)))
+             (chdir-cmd (format " cd '%s'" cd-to)))
          (if ssh-to
              (emamux:send-keys
               (format " ssh %s%s"
